{
  "name": "gathered-farewell-pool",
  "version": "1.0.0",
  "private": true,
  "description": "A simple shared-cost pool tracker.",
  "scripts": {
    "dev": "tailwindcss -i ./styles/input.css -o ./dist/output.css --watch",
    "build": "tailwindcss -i ./styles/input.css -o ./dist/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
